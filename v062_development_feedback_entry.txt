Version: v0.62
Date/time: 2026-05-17 14:16 Berlin time

What was implemented:
- Completed a focused cleanup pass for the v0.61 misses before moving to Phase 4.
- Fixed the Contact/report form path so the app uses the correct FormSubmit Cloud endpoint only, with AJAX/no-redirect behaviour and hidden iframe fallback.
- Added a clear X inside the public search box.
- Reinforced tag chip stability so chips stay in place and grey out instead of disappearing.
- Made the mobile top bar sticky and added mobile overflow guards for results, filter chips, cards, Featured, Profile, and static page containers.
- Fixed shortlist share icon mapping so share uses the megaphone/share symbol rather than link.png.
- Fixed the Featured page to use Priority values only and added the agreed Priority data to listings.json.
- Changed shortlist-detail back navigation so it returns to My Shortlists rather than Results.
- Removed/hidden the duplicate lower mobile Profile inventory and the Hi/profile explanation block; kept the main profile inventory on the first mobile profile screen.
- Added a save icon next to the profile name field.
- Fixed admin highlighter buttons with actual row/column toggle functions and delegated event handling after table rerenders.
- Added Cruising Guide to the admin HTML/page editor list while keeping Featured excluded because Featured is dynamic.
- Reinforced admin horizontal scrollbar styling and reduced clipboard/menu text sizing.

Files changed or created:
- index.html
- style.css
- app.js
- admin.html
- admin.js
- system_info.json
- listings.json
- v062_validation_report.txt
- v062_development_feedback_entry.txt
- v062_debug_checklist.txt

What was fixed:
- Stale report/contact submit behaviour.
- Missing search clear control.
- Sticky/overflow problems on mobile.
- Featured page showing empty despite priority logic being expected.
- Shortlist detail back button going to the wrong destination.
- Duplicate mobile profile summary/helper copy.
- Missing profile inline save control.
- Broken admin highlighter icons.
- Missing Cruising Guide entry in the admin page editor.
- Admin scrollbar/clipboard usability polish.

What still needs checking:
- Submit one real Contact/report test from the live GitHub Pages deployment and confirm it arrives.
- Check mobile scrolling on Results, Featured, Shortlist detail, and Profile on a physical Android device.
- Check Featured page contains the expected priority venues.
- Check admin highlighter icons still work after switching between venues/events and after filtering.

Known issues or limitations:
- FormSubmit no-cors requests cannot expose the remote response code/body in the browser, so success is based on local request completion, not reading the remote server response.
- Full GPS radius, Supabase, live views, and Phase 4 functions were intentionally not implemented.

Clear next recommended step:
- Deploy v0.62, run the focused v0.62 debug checklist, and only move toward the first release / Phase 4 once these repeated cleanup issues are confirmed fixed.
